import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
    root: { transform: 'scale(1.27)', alignSelf: 'flex-end' },
    cardRoot: { transform: 'scale(0.842)' },
    inactiveRoot: { pointerEvents: 'none' }
}));
