export default ({ spacing, palette, shape }) => ({
	root: {
		padding: 0,
		display: 'block',
		borderBottom: '1px solid rgba(0, 0, 0, 0.12)'
	},
	border: {
		padding: spacing(2),
		display: 'block'
	}
});
