const styles = ({ palette, spacing }) => ({
    root: {
        display: 'block',
        minWidth: spacing(2),
        fontWeight: 700,
        textTransform: 'none'
    }
});

export { styles as default, styles };
